# VoiceMem 右脑判断合并与更新一致性：测评实验计划

日期：2026-09-11。状态：拟执行，非实验报告。

## 1. 研究问题与证据边界

本研究有可执行的低成本起点，但现象严重程度尚未测量。核心问题：同一用户、同一类别内，仅用语义向量相似度决定是否复用旧判断，是否会把逻辑不等价的判断及证据合并；发生偏好变化后，系统是否仍使用失效的旧判断？

两个问题分别验证，不能互相替代：

- RQ1 合并正确性：本应区分的判断是否返回同一个 trait ID？
- RQ2 更新一致性：即使没有误合并，旧判断是否仍被检索并影响回答？

当前公开代码的 `TraitStore.add()` 在同用户、同 slot 内按向量相似度寻找已有判断，阈值为 0.95；合并分支追加证据、更新时间。`brain.py` 有针对 heartnote 的旧况标记逻辑。它们是代码观察，不是误合并率或性能结论。[判断表](https://github.com/xzf-thu/VoiceMem/blob/main/voicemem/rightbrain/traits_store.py)、[右脑实现](https://github.com/xzf-thu/VoiceMem/blob/main/voicemem/rightbrain/brain.py)

开始前固定 upstream commit。若实际版本已改动机制，应写出差异并调整研究对象，不强行复现对旧代码的推测。`main` URL 只用于发现来源，不能代替版本标识。

本轮测文本记忆链路；不声称测得声学情绪识别、真实全双工体验或生产可靠性。VoiceMem 论文的算法描述、当前代码版本与本研究新增干预必须分开标注。

## 2. 假设与可证伪结果

| 编号 | 假设 | 能削弱或否定该假设的结果 |
|---|---|---|
| H1 | 在预先固定的非等价判断集合中，原阈值会发生误合并 | 未观察到误合并，且置信区间足够窄；只支持该样本范围的低风险结论 |
| H2 | 部分错误来自合并机制，而非上游抽取 | 相同抽取结果下替换合并策略不改善存储正确性；错误主要在抽取阶段 |
| H3 | 新旧判断并存或旧况状态不一致会增加过时回答 | 在受控重放中，修正旧判断状态并未改善答案，或原系统已正确处理 |

不要把 H1 的零错误当成 H3 不存在。不要把一个反例当成常见失败，也不要把 50 道问答的变化当成普遍性能提升。

## 3. 数据来源及任务映射

| 来源 | 计划用途 | 原始字段或标签 | 限制 |
|---|---|---|---|
| PersonaMem-v2 | 核心偏好更新及对话重放 | preference、preference_updates、对话和 QA | 英文合成数据；更新对不等于同义合并标签，需人工复核 |
| PersonaMem-v1 | 可选外部验证 | 最新偏好、完整演变、变化原因等问题 | 不直接提供本实验全部中间过程标签 |
| LongMemEval cleaned | 外部更新验证 | knowledge-update、single-session-preference、时间与答案 | 通用事实更新不等于情感更新；oracle 会话需重新按时间排序 |
| OCNLI | 中文矛盾压力测试 | entailment / contradiction / neutral | 蕴含不等于等价；开发集有标签，测试标签不公开 |
| PAWS-X | 同义与高词汇重叠非同义对照 | paraphrase / non-paraphrase | 通用句对与人格短语有域差异，不能估计真实写入频率 |

来源与许可核验入口：

- [PersonaMem-v2 数据卡](https://huggingface.co/datasets/bowen-upenn/PersonaMem-v2)，[原始数据目录](https://huggingface.co/datasets/bowen-upenn/PersonaMem-v2/tree/main/data/raw_data)：数据卡标注 CC BY 4.0。下载所选 persona 的文本 JSON 即可；本轮不用图像。
- [PersonaMem-v1](https://huggingface.co/datasets/bowen-upenn/PersonaMem-v1/tree/main)：数据卡标注 MIT；优先 32k 文件。
- [LongMemEval 官方说明](https://github.com/xiaowu0162/LongMemEval)，[cleaned 数据](https://huggingface.co/datasets/xiaowu0162/longmemeval-cleaned)：数据卡标注 MIT；先用 oracle 证据会话作诊断，完整历史成绩另报。
- [OCNLI 官方仓库](https://github.com/CLUEbenchmark/OCNLI)：CC BY-NC 2.0；使用有标签的开发集，排除无共识的 `-` 标签。
- [PAWS-X 官方说明](https://github.com/google-research-datasets/paws/tree/master/pawsx)：下载时记录压缩包中实际许可，未核实前不重新发布数据正文。

以上为公开页面核验，尚未在本项目下载并逐条审计。ChatMem-400K 不作为数据前提。论文总样本量不能当作满足本实验筛选条件的可用数量。

### 3.1 第一批样本目标

- PersonaMem-v2：200 对经审核的偏好更新判断，尽可能来自至少 100 个 persona。
- PAWS-X 中文：100 对同义、100 对非同义。
- OCNLI 中文：200 对 contradiction；不把 entailment 自动作为应合并正例。
- 合计目标 600 对；每个来源分别报告，不给出掩盖语言/域差异的单一总体正确率。

先用不进入正式集合的每来源 10 条检查 schema，再固定随机种子和采样规则。核心来源按 persona、OCNLI 按 prem_id、PAWS-X 按可追溯原始句对/重复文本分组，约一半开发、一半锁定测试；同一组及其改写、翻译不能跨集合。不得按相似度高低筛选主测试集；可另建有明确标识的困难子集。

不足目标数量时记录筛选流程、缺失数和原因，不复制样本充数。PersonaMem 中若大量条目不属于右脑五类，保留筛除统计，不强行转换事实为人格。

通用句对设置同 user/slot 的实验属于“强制同类别压力测试”。另外报告真实抽取器产生同 slot 的覆盖率与子集结果，不能把这两种测试混为实际系统误合并率。

### 3.2 标签与人工复核

先标逻辑关系，再标存储操作：

- equivalent：同对象、同时间/情境范围、同立场的等价表达；可复用判断并追加证据。
- contradiction_same_scope：相同范围内相反立场；不得作为同一判断的支持证据合并。
- temporal_update：明确偏好变化；保留历史，当前判断采用新状态。
- contextual_difference：适用对象、时间或情境不同；保留或细化条件。
- unrelated：无关判断；分开存储。
- ambiguous：无法确定；不进入主要二元指标，单独报告数量。

两位标注者独立标关系与操作，分歧裁决；报告原始一致率及 Cohen's kappa。标注者不看相似度、策略输出或系统答案。先用 30 条开发样本对齐标准，再锁定手册。没有第二位标注者时只能标为单人探索性诊断。

对 PersonaMem 先测试其原始 preference 短语，再测试 VoiceMem 真实抽取的 claim；分别保留原文，禁止为了通过类型检查手工改写后不留记录。跨语言结果分开，翻译扩展不纳入第一轮。

## 4. 阶段 A：判断对与实际存储操作

实现目标：直接调用固定版本的 `TraitStore.add()` 与真实注入的 embed 回调，不用自行重写的余弦规则冒充原实现。

每一对、每个策略创建独立临时数据库：写 A，再写 B，记录两次返回 ID、数据库快照、规范化前后 claim、实际编码字符串、余弦分数和证据所属 ID。清空数据库后交换顺序。两种顺序属于同一实验组，不当作两份独立样本。

双句模式后，可选追加“含同类干扰判断”的微型库，检查最佳候选选择；干扰数量和构造规则必须先固定，单独汇报。

运行时核验 VoiceMem 的归一化、embedding 模型 revision、前缀、精度和注入调用链。[本地 E5 实现](https://github.com/xzf-thu/VoiceMem/blob/main/voicemem/leftbrain/local_e5_embedder.py)

| 策略 | 定义 | 用途 |
|---|---|---|
| B0 原实现 | 固定 commit，原默认阈值与规范化 | 主基线 |
| B1 精确匹配 | 仅规范化后完全相同的 claim 合并 | 简单保守基线 |
| B2 阈值调整 | 开发集比较 0.95、0.97、0.99，锁定一个阈值后测测试集 | 判断是否仅需调阈值 |
| B3 始终新增 | 不合并 | 零误合并但高冗余的参照 |
| C1 随机控制 | 同类别候选内，以开发集 B0 的合并率随机决定合并；种子固定 | 排除仅改变合并数量的解释，不作部署建议 |
| M1 关系门控 | B0 候选先过门槛，再调用固定判断器，仅 equivalent 允许合并 | 有失败证据后才实施的候选干预 |

B2 在开发集选择同义合并召回率不低于 B0 减 5 个百分点、误合并率最低的阈值；平局取较低阈值。没有可行点则保留 B0，并报告阈值调整的代价。测试集禁止重新选择。

M1 判断器只看两个 claim 与允许的上下文，不看 gold 标签或未来对话。输出固定枚举；超时、格式异常记为错误且回退“分开保存”，同时报告回退比例，不能把判断器失败当成方法成功。

## 5. 阶段 B：按时间重放偏好变化

从 PersonaMem 开发部分先选 10 段用于接入，正式测试目标 50 段、至少 25 个 persona，全部来自锁定测试用户。包括持续改变、当前例外、明确纠正和不变偏好；不足时如实记录，不用自造数据混补。相反写入顺序只用于阶段 A，时间重放必须保留真实先后。

第一步固定并缓存上游抽取输出：同一段对话所有策略接收相同 claim、slot、情绪和证据。这样隔离存储策略的效果。第二步才用完整原始对话跑一遍端到端，并区分抽取失败与存储失败。

逐事件保存：heartnote、trait、evidence、旧况标记、检索结果、最终送给回复模型的实际记忆文本。更新前后分别问一个检查问题；问题与答案不得进入后续写入历史。

对每次更新比较“写入完成后”和“清理/会话维护完成后”的状态。主结果等待后台任务完成，不把尚未完成的异步操作误判成永久更新失败；另行记录可见性延迟。最终报告哪些维护函数实际执行、修改了哪些表。

RQ2 的诊断干预独立于 M1：若旧 trait 残留，实验副本中按人工标注暂时屏蔽已经失效的 trait，再运行相同检索。此为 oracle 上界，不能当可部署方法；不能顺便替换抽取器、top-k 或回答模型。

## 6. 阶段 C：问答影响及第二设置验证

仅当阶段 A/B 找到可复现错误，或预先抽样的问答暴露独立更新问题时扩展。先复用阶段 B 的 50 段，再从 LongMemEval 的相关更新/偏好类增加最多 50 题作外部验证。只取证据会话属于诊断结果，不能称为完整 benchmark 分数。

主比较固定回复模型、提示模板、最大记忆条数与 token 预算、解码参数、事件历史和提问；同一问题执行所有策略，顺序随机交错。默认检索 top-k=5，但必须核验右脑实际配额与截断逻辑，日志保存最终可见内容。temperature=0 仍不能假定 API 完全确定；主试验一次配对运行，预先抽取 10 段各重复 3 次估计波动。

阶段 A 的第二设置为第二种公开嵌入模型，待集群核实可用权重、许可后固定名称/revision；它不是第一阶段启动条件。不同模型的阈值只能在开发集校准，同时保留统一 0.95 的诊断结果，禁止直接归因于模型能力。

## 7. 指标与统计

| 指标 | 定义 |
|---|---|
| 误合并率 FMR | 非等价且标签确定的输入中，被复用为同一判断的比例 |
| 同义合并召回率 EMR | 等价输入中正确复用已有判断的比例 |
| 已合并条目的错误比例 | 错误合并数 / 全部合并数；没有合并时记 N/A |
| 证据错挂率 | 支持/反驳关系不正确的证据链接数 / 被评估链接数 |
| 旧判断暴露率 | 更新后问题中，实际模型上下文把失效判断呈现为现状的比例 |
| 当前偏好问答正确率 | 按当前有效偏好回答正确的问题比例 |
| 历史保留正确率 | 需要追溯历史时，能区分过去与现在的问题比例 |
| 开销 | 墙钟时间、p50/p95 写入与检索耗时、额外模型调用、输入/输出 token、峰值内存/显存 |

同时报告原始计数、按来源/关系/语言分层的结果、失败请求、空抽取、类别不符与排除项。证据多的用户不能支配总体结果，提供用户宏平均。

简单比例给 Wilson 95% 区间；多个样本共享 persona/premise 时，以组为单位进行 2,000 次 bootstrap，配对差异也按组重采样。零错误仍给区间，不写“没有风险”。50 段问答属于 pilot；是否扩大样本根据 pilot 的基准率、组内相关与预期效应另做功效分析，不能事后删样本达到显著。

问答优先采用可明确核对的目标偏好与时间范围；需要语义判断时，盲评且隐藏策略名称，人工复核全部策略分歧和随机 20% 一致样本。报告人工与自动判分一致率；不让方法判断器成为唯一裁判。

## 8. 公平性、资源与停止条件

不训练基础模型。阶段 A 原规则及简单基线不调用生成 LLM。所有策略使用相同数据、候选范围与缓存向量；M1 额外调用如实计费，不声称等计算预算。方法比较同时给质量—成本曲线，并检查更严格阈值是否已足够。

以下是规划资源请求上限，不是性能承诺或自动分配：

| 作业 | 建议申请 | 第一批时间上限 | 预算 |
|---|---|---|---|
| 数据审计、阶段 A | CPU 4–8 核，RAM 16 GB，磁盘 10 GB | 4 小时/作业 | 生成 API 0 次 |
| 可选 GPU 编码 | 单卡、申请 8 GB 显存起，RAM 16 GB | 2 小时/作业 | 不训练；小批量起步 |
| 关系门控、阶段 B/C | CPU 4–8 核，RAM 16–32 GB，外部模型或已有服务 | 8 小时/可续跑作业 | 全部 API 含重试累计不超过 2,000 次且总输入输出不超过 5M token，任一先到停止 |

先在 10 段上记录实际 token、内存与耗时，再推算余下预算。模型名称、价格与货币上限由执行环境登记；未配置模型或预算时跳过收费阶段并标 blocked，不影响阶段 A。无需自行申请多卡或部署 35B。

阶段门槛：

- 数据/schema/编码路径不一致：停止结果统计，先修复复现条件。
- H1 无反例且样本区间仍宽：报告“不确定”，可扩样；不得直接进入复杂模型设计。
- H1 有反例：先定位规范化、编码、类别、近邻选择或合并分支；人工确认后进入固定抽取的阶段 B。
- M1 若在锁定测试上降低 FMR，却使 EMR 比 B0 下降超过 5 个百分点，不能称为无代价改进；若 B1/B2 达到相同质量且更便宜，优先简单方法。
- 问答若没有稳定改善：只报告存储层现象，不宣称体验提升。负结果仍产出适用阈值、错误边界与数据审计。
- 超预算、OOM 或持续 API 失败：保存 checkpoint 与失败清单；不得静默跳过后只报成功样本。

## 9. 四周里程碑

1. 第 1 周：锁版本、审数据、双人标注、完成阶段 A 原规则及简单基线。
2. 第 2 周：冻结开发集决策，完成锁定测试与错误分类；决定 H1 是否值得继续。
3. 第 3 周：固定抽取的时间重放，区分合并与旧判断残留；有依据才接关系门控。
4. 第 4 周：完成问答 pilot、第二设置或外部数据验证，发布包括负结果的报告。

四周 go/no-go：只有出现可复现、非抽取独占、跨独立样本成立且简单基线未解决的错误，才把“新更新机制”推进为后续方法研究；否则缩小为诊断报告。

## 10. 复现与交付

每次运行记录代码 commit、补丁、数据 revision/文件 SHA-256、样本 ID、划分清单、模型 revision、依赖锁、硬件、种子、完整配置、提示模板 hash 与运行时间。生成模型权重保持冻结；记忆存储前后快照可比较。

必交：`preflight.json`、`data_audit.json`、`manifest.jsonl`、`annotations.jsonl`、`pair_results.jsonl`、`replay_results.jsonl`、`metrics.json`、`REPORT.md`。未执行阶段用明确状态记录，不伪造空文件代表成功。

仓库只提交脚本、配置、数据来源/ID、允许发布的标签与汇总结果；原始数据、数据库、模型权重与完整日志留在集群产物目录。未核实再分发许可的数据只提交获取方式与 hash。API 密钥不进入配置、日志或 Git。
