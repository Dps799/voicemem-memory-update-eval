# 下一阶段：预算约束下的更新、检索与遗忘实验计划

版本：2026-09-14。状态：待执行方案，不是新增实验结果。
依据：跨机器复现提交 `452f295b734647c9417ed27cb9ef5e99b5bf0ff2`；VoiceMem 固定 `a450911fc8cbb44c46d810aace2f3288bad287e4`。

本文件细化下一轮执行顺序；原 [实验计划](EXPERIMENT_PLAN.md) 的 600 对目标、开发/测试隔离及 2,000 次/5M token 总调用上限继续有效。新增预算扫描为独立诊断，不把原 smoke 结果重新包装成正式测试。当前执行器只支持 smoke；下列新增 runner、标注和正式采样尚未实现。关键数量与上限同时记录在 [next_phase_config.json](next_phase_config.json)，该文件为计划配置，当前 runner 尚不消费它。

## 1. 本轮要回答什么

主要问题：**在固定记忆存储与检索上下文预算下，能否减少过时信息对当前判断的干扰，同时保住有效偏好和必要的历史？**

成功不等于把合并率降到零，也不等于把所有旧记忆删除。需要证明改善来自更新与选择策略，并优于阈值调整、精确匹配、始终新增和简单淘汰等基线。

### 已有观察与未知项

| 已观察到的事实 | 尚不能推出的结论 |
|---|---|
| 45 个句对的结果跨两套环境复现；B0 非 temporal FMR=9/21 | 真实写入中有 42.86% 出错 |
| 原开发集 10 个 temporal 对来自一个 persona | 偏好更新在广泛用户中同样严重 |
| 第二批 10 个更新来自 5 个 persona；3 个更新复用旧 ID | 新信息彻底丢失；实际新证据仍在 |
| 自然问题下，B0 有 2/10 个旧 claim 进入画像指导文本 | 最终回答必然采用旧偏好 |
| 0/20 条背景下相关计数相同，新 claim 显式召回低 | 已经测清记忆竞争或有限预算效果 |

来源：[审计报告](reports/REPORT.md)、[阶段 A 指标](reports/metrics.json)、[阶段 B 指标](reports/replay_metrics.json)、[环境与产物校验](reports/preflight.json)。本方案不新增文献新颖性主张；外部 benchmark 的用途沿用已记录的 [扩展协议](BENCHMARK_ADDENDUM.md)。

## 2. 候选解释与区分实验

| 候选解释 | 可区分的预测 | 会削弱该解释的结果 | 实验 |
|---|---|---|---|
| H-N：规范化删掉否定、对象或条件，造成表面误合并 | raw 与 normalized 标签改变；保留完整文本后错误减少 | 规范化保真样本仍有同类错误 | N1 双层标注及预处理消融 |
| H-M：相似度不能替代逻辑等价 | 同语境、独立复核的非等价对仍被合并 | 复核后错误基本消失，或廉价阈值已足够 | N1 主要对照 |
| H-R：新记忆未被召回或被阈值挡住 | 正确新状态在库中，却在候选/过滤阶段消失 | 候选与呈现新信息充分，错误发生在回答端 | N2 分阶段检索追踪 |
| H-T：缺少时效处理使旧状态被当成当前 | 屏蔽 gold 已失效判断改变呈现或回答，同时历史查询仍需旧状态 | 屏蔽只损害新证据/历史，或没有可见收益 | N2 oracle 过滤诊断 |
| H-B：有限存储/上下文使冗余或旧状态挤出有效记忆 | 固定预算下，简单保守策略不再免费；有效保留和时效出现权衡 | 放宽预算无影响；简单淘汰已达到相同质量 | N3 两种预算分开扫描 |
| H-A：回复模型能利用新证据修正旧 claim | 即使呈现冲突，答案仍采用当前有效偏好 | 同一问题配对条件下出现稳定过时回答 | N4 固定模型问答 |

这些均为待检验候选，可以同时成立。oracle 只作诊断，不能作为可部署方法，也不能把已观察到的案例当作未见测试。

## 3. 数据划分与标注：先于任何新测试输出

### 3.1 正式句对目标保持 600

| 来源 | 目标 | 用途 | 分组单元 |
|---|---:|---|---|
| PersonaMem-v2 | 200 对，目标至少 100 个 persona、每人最多 2 对 | 偏好变化 | persona |
| PAWS-X zh | 100 同义＋100 非同义 | 等价与高重叠压力 | 重复/共享原句的连通分量 |
| OCNLI | 200 contradiction | 同范围矛盾压力 | prem_id，并合并跨组重复文本 |

- 保留现有数据 revision；新增数据只从其固定版本中采样。来源、许可与筛除数逐项记录。
- 已看过的 45 对及其文本重复组、persona604 和第二批涉及的 5 个 persona 从新的确认性集合排除；仍可保留为回归集。
- 先按组约 50:50 划分开发/测试，再在各组内选样。平衡只是目标，不能拆组凑精确条数。
- 主样本不按 embedding 分数筛选。高相似困难集另外标注为 challenge，不代替主测试。
- 锁定抽样种子 20260914；方法随机对照继续用 20260911/12/13。记录全部候选及排除原因，数量不足则报告短缺。
- 样本是句对/用户，方向、策略、背景规模和重复运行都不是新增独立样本。

### 3.2 标签必须同时覆盖原文和规范化文本

标注表至少包含：source_id、group_id、原始 A/B、规范化 A/B、对象、时间范围、条件、关系、预期操作、置信/歧义、标注者、裁决理由。对象和时间标签供评测器使用，默认不输入方法。

关系：equivalent、contradiction_same_scope、temporal_update、contextual_difference、unrelated、ambiguous。

操作：merge_support、add_separate、supersede_current_keep_history、retain_contextual_exception、undetermined。

两位标注者独立工作，不看相似度、策略结果或另一位标签；先用另取的 30 个开发案例统一规范，再锁定手册。分歧人工裁决，报告一致率与 kappa。未有第二位标注者时可以完成工程和单人诊断，但不能写成双人确认性结论。

必须逐条回答：

1. 原文与规范化文本是否表达相同意思？若删掉条件，定位到具体片段。
2. 新旧判断是否同对象、同时间/情境范围？“现在不想听建议”不自动等于永久性偏好改变。
3. 新证据是在支持、反驳、修正，还是增加例外？数据库 ID 连接正确不等于语义支持正确。
4. 来自 preference_updates 的标签是否足以判定有效状态？不充分的样本标 ambiguous，不能为达到样本目标硬判。

N1 分开报告：原文 gold 下的整条处理链失败；规范化后重新复核的表示级合并失败。两者都保留，并交叉标出由规范化引入的语义损伤，不能删掉这类失败后只报更好结果。

### 3.3 一次性锁测试

在开发部分完成 N1/N2/N3 的规则、主指标、阈值、预算和提示选择后，统一冻结测试配置再打开对应测试结果。N2/N3 测试 persona 必须属于全局测试组。若先打开 N1 测试并据此改方案，后续确认性实验必须另留未见测试组；已看结果降级为探索分析。

## 4. N0：执行器和环境准备

交付 `formal_manifest`、`split_manifest`、`annotation_template`、`next_preflight` 和可检查配置，先不跑测试策略。

- 当前固定依赖为参考环境；集群实测新版依赖作为第二环境单独登记。模型 revision、前缀、精度和向量维度固定；不要求为了 CPU 实验先修多卡 CUDA。
- 让正式 runner 读取 manifest 和配置；不可复用 record_run.py 中写死的 570/160 验收数。验收数应从输入数量、条件集合和排除规则推导。
- 将每次实验放入唯一 run_id 目录，不覆盖已发布运行；记录 manifest、配置、代码、上游、提示与输出哈希。
- 修复聚合器未关闭文件的小警告；新增关键测试：split 无泄漏、评测 query 不回写、gold 不进入策略、预算截断可复现、oracle 单独标记、失败不算成功。
- 完整集成前获取固定 upstream 完整代码与依赖；现有 AST 画像切片仍用于单元诊断，不能直接改名为完整 VoiceMem。

验收 G0：同配置可重跑；输入分组不交叉；每条结果可追溯到输入；模型异常导致整条失败被记录，不被算作正确拒绝。

## 5. N1：正式判断对诊断

### 对照与消融

主对照 B0、B1、B2、B3、C1；M1 暂不作为开始条件。

- B2 仅在开发集选择 0.95/0.97/0.99；EMR 不低于 B0−5pp 的候选中 FMR 最低者，平局取较低阈值，无可行改进则保留 B0。
- C1 合并概率仅由开发集 B0 eligible 合并率得出，冻结到测试集，不用测试合并率重新校准；三种子全部报告。
- 每对 AB/BA 都跑，但合并决策、保留的 claim 和证据链接分别比较；不得以 merged 相同声称状态顺序无关。
- 预处理消融只在开发集进行：原 normalize 与保留全文版本配对，其他配置不变。保留全文为实验干预，不能替代原基线。
- 不把两个英文/中文来源的率混成一个“系统准确率”。主要按来源、关系和规范化损伤分层。

指标：非 temporal FMR、temporal 合并率、EMR、包含 temporal 的错误合并占比、语义损伤率、证据支持冲突率；均附分子/分母、排除数、错误数和组级区间。

计划成本：按最多 5 个确定性配置（开发阶段包括两个 B2 阈值候选）估算，600×5×2＋600×3=7,800 条运行记录为资源上界（控制项另计）。测试只运行冻结的一个 B2；若开发/测试各 300 对，则实际主对照约 300×5×2＋300×4×2＋600×3=7,200 条。缓存固定向量；不把缓存命中后的写入时间说成包含编码的总延迟。

验收 G1：要进入新机制研究，应在排除规范化/歧义解释后，观察到来自多个独立组的非等价错误，且简单 B1/B2 的质量与代价存在未解决权衡。若只有通用中文压力集失败、PersonaMem 未确认，缩小结论范围；不直接推广为情感记忆失败。

## 6. N2：有背景竞争的时序与检索诊断

### 6.1 样本和问题

- 开发：目标 12 个新的开发 persona，每人最多 2 段，用于集成与冻结配置。
- 测试：50 段、至少 25 个未见 persona，每人最多 2 段。与 N1 测试 persona 可以共享，但必须共同冻结，不计为独立外部复现。
- 覆盖持续改变、明确纠正、临时例外和不变偏好四类。先审计实际覆盖；缺失类别只能另建受控集并分表报告，不用合成条目冒充自然数据。
- 每段 4 个冻结问题：2 个当前状态问题（不同表达）、1 个历史问题、1 个应保持不变的偏好问题。主测试共 200 个问题，按 persona 聚合。
- 不把 gold 新 claim 原句当自然问题。问题不泄露正确状态，问答探针不写回历史，不更新检索访问计数。

记录实际事件先后；以数据中的观测时间为准，不能把文件排列当时间。缺少可靠时间的样本只进入受控序列诊断。

### 6.2 背景如何构造

主比较增加 20/100 条背景；0 条仅保留诊断。背景按嵌套列表增加，保证不同预算共享相同输入。

优先使用目标 persona 在该问题时刻已经发生的真实记忆。另设明确标记的受控背景：同主题近邻约 50%、相关但不同对象/条件约 25%、无关项约 25%。比例是设计目标，记录可用量及实际组成。

困难背景可以根据开发集确定的 embedding 检索规则选择，但只访问当时可见历史，不使用 gold 答案、未来更新或测试表现选背景。不能从别人的偏好直接搬来充当目标用户事实；如用合成背景，完整标注其来源并单独报告。

### 6.3 条件与真实调用路径

主比较三条链：B0、开发集锁定的简单基线 S（B1/B2/B3 中选择）、O_filter。

S 选择规则：开发集优先满足 EMR≥B0−5pp、有效信息保留不低于 B0−5pp；可行者中旧 claim 暴露最低，平局取额外计算最少、再取存储较小者。若无可行者，仍报告 B1 的代价作为诊断，但不称为改进；选择只执行一次。

O_filter 在 B0 的固定候选上屏蔽 gold 明确已失效、且不适用于当前问题的判断，再走相同配额和呈现。它是依赖答案时效标注的 oracle 诊断，不修复已经误合并的存储，不保证构成所有方法的质量上界。历史问题不能一律过滤旧记忆。若删掉旧 claim 也删掉唯一的新证据，要明确记录这种损失。

两条路径分别报告：

1. **固定判断的画像链**：原始候选 → 相似度过滤 → 来源配额 → 记忆 token 打包 → 画像指导文本。无需生成模型。
2. **完整检索链**：缓存真实抽取、heartnote/trait/左脑结果、实际后台维护和最终模型 prompt。只有固定完整代码与模型服务可用后执行；尚未调用的部分标未运行，不使用 AST 切片冒充。

每条轨迹保存：写入前后快照、存储 claim、完整证据、有效时间、候选排名/分数、过滤原因、配额丢弃、预算丢弃、实际可见文本。更新完成和后台维护完成分开记录；主结果等待维护完成，异步可见性延迟另报。

### 6.4 上下文预算

- 预算为 **128/256/512 个记忆 token**，开发主点 256；保持检索候选数、阈值和来源配额相同，只改变最终打包预算。
- 使用固定 E5 tokenizer revision 作为 CPU 诊断的计数单位，必须明确不是回复模型 token。完整问答阶段改用冻结 answerer tokenizer/服务计数，并重新记录实际可见文本，不直接沿用诊断 token 数。
- 以完整“claim＋证据＋时间标签”作为不可拆分单元，按固定排序依次装入，超长单元跳过并记录；不能截断否定、条件或时间来凑预算。若与原版格式不同，单列为预算适配，原版渲染作为参照保留。
- 50×4×3 策略×2 背景规模×3 预算=3,600 条主画像检索记录，属于 50 段的重复条件。

### 6.5 关键指标和错误分解

| 层次 | 指标及分母 |
|---|---|
| 写入 | 新证据存在率；当前状态被独立/正确表示率；支持/反驳冲突链接数/检查链接数 |
| 候选 | 当前有效信息在 top-k 的比例，仅在库中存在有效信息的问题上定义，并同时报告全问题率 |
| 过滤 | 有效信息已在候选却被阈值/配额去掉的问题数/有有效候选问题数 |
| 呈现 | 旧状态未注明过时而呈现为当前的问题数/当前状态问题数 |
| 呈现 | 当前有效信息可见率：同时检查 claim 与 evidence，不能只比较 claim 字符串 |
| 历史 | 历史问题中必要旧信息和时间关系可见的问题数/历史问题数 |
| 保护 | 未变化偏好仍可见的问题数/对应保护问题数 |

“是否以当前身份呈现”需结构化状态标签或盲审，不能仅凭旧字符串出现判错。各阶段丢失原因允许多标签，同时给第一个丢失阶段以避免重复归因。

验收 G2：从主当前问题中定位到可人工确认的过时呈现；或显示新信息主要在过滤阶段丢失。若 oracle 过滤无改善而召回很低，下一步优先研究召回与阈值校准，不强行上遗忘模型。若背景完全不影响候选/预算，判为竞争设计不足，先在开发集修正，不能声称预算鲁棒。

## 7. N3：有限预算的长期更新与遗忘

N2 的开发结果决定是否启用，测试配置仍需在测试打开前冻结。目标是测到“保存一切”也会付出代价，同时区分合理保留历史与错误沿用历史。

### 7.1 两类预算不可混淆

1. **持久记忆预算**：对方法可检索、可读取的序列化 claim、scope、时间与 evidence 文本计 token。主点 2,048，扫描 1,024/2,048/4,096。向量、索引和 DB 实际字节另报，不能只限制文字却声称总存储相同。
2. **检索上下文预算**：主点 256 个诊断 tokenizer token，扫描 128/256/512。

先固定上下文扫描持久预算，再固定持久预算扫描上下文，合计 5 个不同预算条件；不做重复中心点或无必要的全因子 3×3。

被淘汰内容只存在独立评测档案，方法、检索器、摘要器和回答模型都不得访问；否则不算实际预算约束。常驻元数据与旧 evidence 不能藏在未计费缓存里。

### 7.2 长历史构建

以 N2 的 persona 为基础，开发先选 12 个，测试使用目标 25 个未见 persona。优先用可验证的真实事件序列；先统计长度分布，不能预设数据都有长历史。

受控压力轨迹另建：40/160 个事件，20/40/80/160 步检查点（短轨迹只含前两点），每个检查点当前/历史/不变各一问。同一来源 persona 的所有轨迹与改写不得跨 split。160 事件主轨迹与 40 事件轨迹若共享前缀，前两个检查点只计一次。

受控集明确注入同义重述、持续更新、短期例外、对象不同的近邻、不变核心偏好；规则及随机种子在开发阶段固定，gold 仅供评测器。生成规则只用于诊断，不能称为真实用户变迁分布或官方 benchmark。

### 7.3 基线与候选方法

每条链由“写入规则＋淘汰规则＋检索规则”明确组成，单次比较只改所需因素：

- P0：B0 合并＋达到预算后 FIFO，检索不变。
- P1：锁定简单写入基线 S＋同样 FIFO，检索不变。
- P2：同 P1 写入＋基于已观测事件的 recency/frequency 淘汰；计数只来自输入历史，不使用评测探针访问。
- P3：候选更新感知策略，仅在 G1/G2 支持且可实现时加入：区分持续纠正、临时例外与同义支持，旧状态保留为有界历史摘要，当前检索按有效范围筛选。所有关系/范围判断必须从当时输入或固定抽取器得到，禁止直接读人工 gold。
- O_filter 或 O_state：oracle 诊断单列，不参与部署方法排名。

P2 的 recency/frequency 权重仅在开发集的预定小网格选择；P3 若依赖关系模型，报告额外调用/延迟，不能宣称与零调用基线等计算。若 P1/P2 已足够，则不进入复杂 P3。

基线无预算上限的结果只作为存储代价参照。主比较中所有方法共享同一预算、输入顺序、tokenizer、候选范围及评测点。

主指标：当前信息可见率、未注明过时的旧状态暴露率、历史和不变偏好保留率、文本 token/总字节、写入与检索 p50/p95、更新后达到一致状态的事件步数/墙钟延迟。回答正确率只在 N4 定义。

25 persona×4 检查点×3 问题×4 可部署策略×5 不重复预算点=6,000 条主检索记录上限；缺失 P3 或短轨迹时按实际配置计数。不把 6,000 当作 6,000 个用户。

验收 G3：在相同预算下看到跨 persona 的可重复权衡或改善，同时报告历史/不变信息代价。若只通过清空历史改善当前指标，或改善完全来自更多模型计算，不认定为预算下的无代价方法优势。

## 8. N4：问答因果诊断与外部验证（条件启动）

开始条件：N2/N3 完成输入/呈现校验，冻结 answerer、提示、tokenizer、解码设置与费用上限。当前模型与货币上限未配置，计划不能视作付费调用授权。

- 先用开发段测真实抽取与单次 token 开销，记录失败重试；在总预算内才继续。
- 主问答使用 50 段×4 问题×3 条链=600 个回答。三条链为 B0、开发阶段选定的可部署 S/P3、明确标记的 oracle 诊断；采用冻结的单个预算点，不对所有网格调用回答模型。
- 每个问题接收相同历史和查询，只有实验记忆不同；模型、系统提示、解码设置相同，策略顺序交错。
- 预先抽 10 个问题、每条链增加 2 次重复，共 60 个额外回答；重复属于同一问题，不扩大独立样本数。
- 不直接把旧 claim 暴露当答案错误。盲评是否使用当前状态、是否区分历史、是否错误抹去有效偏好；保留“无法判断”。
- 所有策略分歧和随机 20% 一致答案由人工复核；方法的关系判断器不能成为唯一裁判。

### 总预算包络（是上限分配，不是已消耗量）

| 用途 | 最大调用数 |
|---|---:|
| 固定真实抽取接入 | 150 |
| 可选 M1/P3 关系诊断 | 200 |
| 主回答 | 600 |
| 主答案自动盲评 | 600 |
| 重复回答 | 60 |
| 重复答案盲评 | 60 |
| 外部最多 50 题×2 方法回答 | 100 |
| 外部答案盲评 | 100 |
| 合计计划 | 1,870 |
| 重试/异常余量 | 130 |

全部调用含重试累计不超过 2,000 次，全部角色输入输出合计不超过 5M token，另需配置货币上限；任一先到即保存断点并停止。长对话抽取若 150 次不足，缩小完整链样本并明确披露，不静默绕过上限。关系模块 200 次不足以支持整个方法网格时，只完成诊断或使用已核验的本地服务并单独计资源，不把未测方法写为完成。

外部验证默认先用 LongMemEval 对应更新/偏好题；只有遗忘方向成立再按原 addendum 接 Memora。最多 50 个外部问题为共享额度，不是每个 benchmark 各 50。Memora 仍遵循先前 30 题 pilot 上限和正确的 MPA/FAA/FAMA 口径。只使用证据会话的诊断不得报告为完整 benchmark 分数。

## 9. 统计、主比较与继续/停止规则

- N1 主要按来源报告 FMR/EMR/temporal 合并率；N2/N3 主比较为固定中心预算下可部署 S/P3 相对 B0/P0 的旧状态暴露差，配套当前信息、历史和不变信息保留差。
- N4 主结局为当前偏好回答正确率；历史正确率与不变偏好为保护指标。oracle 为机制诊断，不参与主要优胜判断。
- 以 persona、premise 或原句连通组为单位做 2,000 次配对 bootstrap；报告用户宏平均、绝对百分点差和 95% 区间。多个预算点共享数据，不能视为独立复现。
- 主要比较与中心预算预先固定；其余网格与分层描述性报告。若追加多个确认性优越性检验，预先登记比较族并用 Holm 校正；不得从网格挑最漂亮的点作为主结论。
- 5pp 的保留下降门槛是工程筛选约束，不是现有样本量能证明的统计非劣界。开发/测试各约 50 个等价样本、25 个重放 persona 都可能区间很宽。
- 本轮人数由资源和可用数据决定，不声称充分功效。用开发 pilot 的差值、组内相关与人工复核率规划后续样本；不看测试显著性反复加样。
- 歧义/缺时间/缺上下文按预定规则排除并报告；运行失败作为失败/缺失单列，不能按策略静默丢弃。问题缺有效记忆时同时报告全问题与条件分母。
- 若提升小于工程可用门槛或区间不确定，结论写“不确定”；若廉价基线已解决，停止复杂模型设计；若只存储改善而答案未改善，研究贡献限定为存储/诊断。

建议继续方法研发的目标门槛（待开发阶段冻结，非已达到）：相同预算下旧状态暴露至少下降 10pp，当前信息/历史/不变偏好保留的点估计下降均不超过 5pp，并且组级区间不支持明显恶化。它是研发决策规则，不替代显著性/非劣效检验，也不保证最终论文价值。

## 10. 资源、排期与任务交付

### 资源申请上限

| 工作 | 资源建议 | 初始作业上限 | 说明 |
|---|---|---|---|
| N0/N1 数据与向量诊断 | CPU 8 核、RAM 16GB、磁盘 10GB | 4 小时/作业 | 无生成模型调用 |
| N2/N3 固定抽取的检索/预算轨迹 | CPU 8 核、RAM 16–32GB | 8 小时/可续跑作业 | 先 5 个 persona 测实际吞吐，再估算 |
| 可选本地关系/回答模型 | 待现有可用模型确认后申请 | 不预先占多卡 | 不训练、不默认部署 35B |
| 人工标签 | 两位独立标注者＋分歧裁决 | 按 pilot 更新 | 600 对×2×45–90秒≈15–30人时，另加裁决；重放需额外核查 |

上述时间是申请上限/估算，不是性能承诺。完整原始数据或全量 DB 若超过 10GB，先分片/压缩再调整申请，不能静默扩额。

### 约两周的工作顺序（人力可用时）

1. 第 1–2 工作日：N0、抽样、泄漏检查、30 对标注手册试标；冻结数据选择规则。
2. 第 3–5 工作日：600 对标注与开发 N1 并行推进；完成规范化归因和简单基线选择。没有第二标注者则明确停在单人诊断。
3. 第 6–8 工作日：N2 开发序列、自然问题、竞争背景、真实过滤与 token 打包；解决输入可见性和时间对齐。
4. 第 9–10 工作日：N3 开发压力轨迹和预算扫描；冻结全部测试配置、主要对照和统计计划后统一测试并交付。
5. 后续独立里程碑：按资源门槛启动 N4；不把等待模型配置的时间承诺为已安排的计算时间。

### 集群任务清单及产物

| 任务 | 必交产物 | 完成判据 |
|---|---|---|
| T0 版本与可执行配置 | next_preflight、run_config、环境锁/实测环境 | 所有实际路径与哈希可追溯 |
| T1 数据与分组 | manifest、split_manifest、exclusions、coverage | 无用户/重复文本泄漏，无 smoke 污染 |
| T2 标注 | handbook、双份 labels、adjudication、agreement | 原文和规范化均覆盖；未裁决不混入主标签 |
| T3 N1 | pair_results、normalization_audit、metrics | 分母/失败/AB-BA 状态差可重算 |
| T4 N2 | event_manifest、query_manifest、retrieval_trace、state_snapshots | 能定位信息在哪一层首次丢失 |
| T5 N3 | budget_usage、eviction_trace、checkpoint_metrics | 淘汰后方法不能读评测档案；预算核算完整 |
| T6 统计与报告 | analysis_config、paired_intervals、deviations、REPORT | 主比较预先冻结，负结果照常报告 |
| T7 可选 N4 | actual_prompts、answers、blind_labels、cost_ledger | 模型/预算已配置，未运行内容有明确状态 |

建议新增目录 `runs/<run_id>/`，公开仓库提交协议、代码、输入 ID/hash、允许发布的标签与汇总。数据正文与完整 prompt 依许可和既有数据策略留在集群；不把 API 密钥或无法再分发的原文推送到公开仓库。

## 11. 第一张正式结果表应该长什么样

每一行注明来源、测试 persona 数、输入事件数、写入规则、淘汰规则、持久 token/实际字节、上下文 token、当前信息可见率、旧状态暴露率、历史保留率、不变偏好保留率、额外调用和 p95 延迟。N4 运行后才新增答案正确率。

最终应能回答：错误是否跨独立样本成立；它来自预处理、更新、召回还是预算；简单方法能解决多少；剩余问题是否影响回答；新方法是否值得其额外计算与存储代价。任何一个环节尚未测量，报告中就保留该未知项。
